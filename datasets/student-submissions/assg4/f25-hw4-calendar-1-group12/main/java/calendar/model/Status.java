package calendar.model;

/**
 * Represents whether a calendar event is public or private.
 * PUBLIC indicates that the event is visible to others, while
 * PRIVATE means that the event is only visible to the owner.
 */
public enum Status {
  PUBLIC,
  PRIVATE
}
