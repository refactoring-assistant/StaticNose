package calendar.model;

/**
 * Represents the visibility status of a calendar event.
 * An event can be either PUBLIC (visible to others) or PRIVATE (visible to the user).
 */
enum EventStatus {
  PUBLIC,
  PRIVATE
}
