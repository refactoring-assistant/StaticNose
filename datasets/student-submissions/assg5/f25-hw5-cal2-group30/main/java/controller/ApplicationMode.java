package controller;


/**
 * Represents 2 modes in which the calendar application can run.
 * HEADLESS mode reads commands from a file.
 * INTERACTIVE mode accepts commands from user input.
 */
public enum ApplicationMode {
  HEADLESS,
  INTERACTIVE
}
