package calendar.controller;

/**
 * Represents a controller for the Calendar, interacts with the Calendar model and view, ensuring
 * their minimal connection.
 */
public interface CalendarController {
  /**
   * Runs the Calendar Controller.
   */
  void run();
}
