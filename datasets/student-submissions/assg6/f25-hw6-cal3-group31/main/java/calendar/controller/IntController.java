package calendar.controller;

/**
 * Interface for a controller that takes input from the user and makes
 * requests from a Calendar in the form of Commands.
 */
public interface IntController {
  /**
   * Starts the controller and processes commands from input.
   */
  void go();
}
