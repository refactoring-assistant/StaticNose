package calendar.controller;

/**
 * Interface for calendar controllers.
 * Handles command processing and coordinates between model and view.
 */
public interface CalendarController {
  /**
   * Starts the controller and runs the command loop.
   */
  void run();
}
