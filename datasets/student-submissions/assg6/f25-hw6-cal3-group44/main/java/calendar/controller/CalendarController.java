package calendar.controller;

/**
 * Represents a controller for the calendar application.
 * The controller is responsible for handling input, executing commands,
 * and coordinating between the model and the view.
 */
public interface CalendarController {
  /**
   * Starts the execution of the calendar program.
   * This method processes user input and delegates actions to the model and view.
   */

  void run();
}
